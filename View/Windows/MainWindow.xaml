<Window x:Class="AutoSound.View.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AutoSound.ViewModel"
        xmlns:pages="clr-namespace:AutoSound.View.Pages"
        xmlns:Page ="clr-namespace:AutoSound.View.Pages.Product"
        xmlns:Page1 ="clr-namespace:AutoSound.View.Pages.Employee"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Title="AutoSound" SizeToContent="WidthAndHeight">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:LoginViewModel}">
            <pages:LoginPage/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ProductViewModel}">
            <Page:ProductPage/>
        </DataTemplate>
    </Window.Resources>
    <Frame x:Name="MainFrame" 
           NavigationUIVisibility="Hidden"
                    Content="{Binding Navigations,UpdateSourceTrigger=PropertyChanged}"/>
</Window>